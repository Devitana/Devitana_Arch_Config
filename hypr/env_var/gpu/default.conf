##################################
### DEFAULT / GENERIC GPU      ###
##################################

# Safe fallback for unrecognised or unsupported GPUs.
# Uses the Vulkan renderer and disables hardware cursors for
# maximum compatibility.

env = WLR_RENDERER,vulkan
env = WLR_NO_HARDWARE_CURSORS,1
